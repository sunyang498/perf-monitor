# perf-monitor 项目知识库

> **目标读者：** 团队大模型（LLM）— 用于代码理解、问答、CR、新人 onboarding。
> **最后更新：** 2026-05-21
> **总代码量：** ~470 行 TypeScript（src/）+ 45 行 JavaScript（server/）+ 配置

---

## 1. 项目概览

| 属性 | 值 |
|---|---|
| **项目名称** | perf-monitor |
| **定位** | 浏览器端性能监控 SDK，`<script>` 标签引入即用 |
| **语言** | TypeScript (src/) + JavaScript (server/) |
| **构建工具** | Rollup 4.x，输出 IIFE 格式 |
| **运行时目标** | 浏览器 (Chrome 58+, Edge 16+, Firefox 89+) |
| **包管理** | npm，`"type": "module"` |
| **产物** | `dist/perf-monitor.js`（单文件，含 sourcemap） |
| **服务端** | Node.js Express 5.x，`server/index.js` |
| **GitHub** | https://github.com/sunyang498/perf-monitor |

---

## 2. 文件清单与职责

```
perf-monitor/
├── src/
│   ├── index.ts              # 入口：init() 配置校验 + 模块串联
│   ├── core/
│   │   ├── metrics.ts        # LCP / FID / CLS 性能指标采集
│   │   ├── error.ts          # JS错误 / 资源错误 / Promise异常 监控
│   │   └── unload.ts         # 页面卸载兜底（visibilitychange + pagehide）
│   ├── transport/
│   │   ├── queue.ts          # 批量队列（生产者-消费者）
│   │   ├── sender.ts         # 双通道发送器 + 弱网状态机
│   │   └── storage.ts        # IndexedDB 离线队列持久化
│   ├── types/
│   │   └── index.ts          # 公共类型定义（配置/指标/错误/传输）
│   └── utils/
│       └── helper.ts         # 类型守卫（isMetric / isError）
├── server/
│   └── index.js              # Express 接收服务（ES模块）
├── test/
│   └── index.html            # 交互式测试页面
├── dist/
│   └── perf-monitor.js       # 构建产物
├── rollup.config.js          # Rollup 配置
├── tsconfig.json             # TypeScript 配置
├── package.json              # 依赖与脚本
├── README.md                 # 项目 README
└── todo.md                   # 任务看板
```

---

## 3. 类型体系

### 3.1 配置类型

```typescript
// 文件：src/types/index.ts
interface PerfMonitorConfig {
    url: string;                    // 必填：上报地址
    batchSize?: number;             // 默认 5：攒够 N 条立即发送
    flushInterval?: number;         // 默认 5000ms：定时发送间隔
    errorDedupWindow?: number;      // 默认 10000ms：相同错误去重窗口
}
```

### 3.2 数据载体（判别联合类型）

```typescript
type MetricType = 'LCP' | 'FID' | 'CLS';

interface MetricData {
    type: MetricType;           // 指标类型
    value: number;              // 指标值（LCP/FID 单位 ms，CLS 无单位）
    pageUrl: string;            // 当前页面 URL
    timestamp: number;          // 采集时刻的绝对时间戳 Date.now()
}

type ErrorType = 'jsError' | 'resourceError' | 'promiseError';

interface ErrorData {
    type: ErrorType;            // 错误类型
    message: string;            // 错误消息
    filename?: string;          // 出错文件 URL
    lineno?: number;            // 出错行号
    colno?: number;             // 出错列号
    stack?: string;             // 错误堆栈
    timestamp: number;          // 采集时间戳
}

type TransportItem = MetricData | ErrorData;   // 队列统一类型
type FlushCallback = (items: TransportItem[]) => void;
```

### 3.3 类型守卫

```typescript
// 文件：src/utils/helper.ts
function isMetric(item: TransportItem): item is MetricData;
function isError(item: TransportItem): item is ErrorData;
```

### 3.4 IndexedDB 记录 Schema

```typescript
// 文件：src/transport/storage.ts（模块内部，不导出）
interface PendingRecord {
    id?: number;                // 自增主键
    items: TransportItem[];     // 一批待发送数据
    timestamp: number;          // 写入时间
}
// ObjectStore: "pending-reports"，keyPath: "id"，autoIncrement: true
```

---

## 4. 架构分层

```
┌─────────────────────────────────────────┐
│ Layer 1: 采集层 (src/core/)              │
│   metrics.ts: observeLCP/FID/CLS        │
│   error.ts:   listenJSError/Resource/    │
│               PromiseError              │
│   产出: TransportItem                    │
├─────────────────────────────────────────┤
│ Layer 2: 传输层 (src/transport/)         │
│   queue.ts:   批量缓冲 (addToQueue)      │
│   sender.ts:  双通道发送 (createSender)  │
├─────────────────────────────────────────┤
│ Layer 3: 持久层 (src/transport/)         │
│   storage.ts: IndexedDB CRUD            │
├─────────────────────────────────────────┤
│ Layer 4: 兜底层 (src/core/)              │
│   unload.ts:  页面卸载紧急 flush         │
└─────────────────────────────────────────┘
```

**粘合层：** `src/index.ts` 的 `init()` 函数负责：校验配置 → 初始化各层 → 注入回调链 → 启动采集。

**模块间依赖方向（单向）：**
```
index.ts → metrics.ts → queue.ts
         → error.ts   → queue.ts
         → sender.ts  → storage.ts
         → unload.ts  → queue.ts
         → storage.ts
```

`queue.ts` 不知道 `sender.ts` 的存在（依赖倒置：通过 `onFlush(cb)` 回调注入）。

---

## 5. 模块接口协议

### 5.1 index.ts — 唯一公共 API

```typescript
// 挂载方式：IIFE 打包后 window.PerfMonitor = { init }
PerfMonitor.init(config: PerfMonitorConfig): void

// init() 执行顺序（严格）:
// 1. 防重复初始化检查
// 2. url 必填校验
// 3. 合并默认值 (?? 操作符)
// 4. setupQueue(config)            ← 启动队列定时器
// 5. onFlush(createSender(config)) ← 注入发送回调
// 6. setupUnload(normalFlush, urgentFlush) ← 注册卸载监听
// 7. 检测 IndexedDB 残留 → addToQueue 补发
// 8. observeLCP/FID/CLS            ← 启动性能采集
// 9. listenJSError/Resource/Promise ← 启动错误监控
```

### 5.2 metrics.ts — 三个导出函数

```typescript
// 无参数，无返回值。内部直接调用 addToQueue()
export function observeLCP(): void;
export function observeFID(): void;
export function observeCLS(): void;

// 内部共享工具：
function buildMetric(type, value): MetricData;
```

### 5.3 error.ts — 三个导出函数

```typescript
export function listenJSError(): void;
export function listenResourceError(): void;
export function listenPromiseError(): void;

// 均无参数无返回值，内部直接构建 ErrorData → addToQueue()
```

### 5.4 queue.ts — 四个导出 + 一个内部

```typescript
export function setupQueue(config: PerfMonitorConfig): void;  // 初始化 + 启动定时器
export function onFlush(cb: FlushCallback): void;              // 注册 flush 回调
export function addToQueue(item: TransportItem): void;         // 生产者入口
export function flush(): void;                                 // 正常 flush → sender
export function drainQueue(): TransportItem[];                 // 取出快照，不调 sender
```

### 5.5 sender.ts — 一个工厂函数

```typescript
export function createSender(config: PerfMonitorConfig): (items: TransportItem[]) => Promise<void>;

// 返回的 send 函数内部状态机：
//   去重 → JSON.stringify → trySend(url, body, 0)
//     → 成功: replayStaleData(url)（补发 DB 残留）
//     → 失败: trySend 内部 saveToDB + sendBeacon
```

### 5.6 storage.ts — 四个导出 + 一个内部

```typescript
export async function saveToDB(items: TransportItem[]): Promise<void>;
export async function loadAllFromDB(): Promise<TransportItem[]>;
export async function clearDB(): Promise<void>;
export function isDBSupported(): boolean;

// 内部工具：
function openDB(): Promise<IDBDatabase>;  // 单例缓存
```

### 5.7 unload.ts — 一个导出

```typescript
export function setupUnload(
    normalFlush: () => void,   // visibilitychange 路径（异步 sender）
    urgentFlush: () => void,   // pagehide 路径（同步 DB + beacon）
): void;
```

---

## 6. 完整数据流

### 6.1 正常采集 → 上报

```
用户交互 / 页面加载
  │
  ├─ PerformanceObserver 回调 (metrics.ts)
  │    └─ buildMetric() → addToQueue(item)
  │
  ├─ window error / unhandledrejection (error.ts)
  │    └─ 构建 ErrorData → addToQueue(item)
  │
  ▼
queue.ts: addToQueue(item)
  ├─ queue.push(item)
  └─ if queue.length >= batchSize → flush()
       │
       └─ 或 setInterval 定时 → flush()
            │
            ▼
         flush():
           snapshot = [...queue]
           queue.length = 0
           flushCallback(snapshot)
            │
            ▼
         sender.ts: send(snapshot)
           ├─ 错误去重 (Map<key, timestamp>)
           ├─ JSON.stringify(filtered)
           └─ trySend(url, body, 0)
                ├─ fetch(keepalive) → 成功 → return true
                └─ 失败 → setTimeout 1s → trySend(retry+1)
                     └─ retry>=3 → saveToDB + sendBeacon → return false
```

### 6.2 页面卸载

```
用户关闭标签页
  │
  ├─ visibilitychange(hidden) → normalFlush = queue.flush()
  │     └─ 走完整 sender 异步重试链
  │
  └─ pagehide → urgentFlush
        │
        drainQueue()  → 同步快照 + 清空队列
        saveToDB()    → 写 IndexedDB（异步但浏览器会完成事务）
        sendBeacon()  → fire-and-forget 发送
```

### 6.3 离线恢复

```
init() 调用
  │
  ├─ isDBSupported() ?
  │    └─ YES → loadAllFromDB()
  │         ├─ 扁平化所有 PendingRecord.items
  │         └─ 逐条 addToQueue(item)
  │              └─ 触发正常 flush 链路
  │
  └─ 正常 flush 成功后
       └─ sender.ts: replayStaleData(url)
            ├─ loadAllFromDB() 再次检查
            ├─ fetch(url, body) 补发
            └─ 成功 → clearDB()
```

---

## 7. 关键设计决策与原因

### 7.1 为什么 Rollup + IIFE 而不是 Webpack + ESM？

- 最终用户通过 `<script>` 标签引入，IIFE 不依赖任何模块加载器
- Rollup 对库打包天然友好，输出无运行时代码冗余
- `name: 'PerfMonitor'` 自动生成 `var PerfMonitor = (function(){...})()`

### 7.2 为什么 fetch + sendBeacon 双通道？

- **fetch + keepalive**：可获取 HTTP 状态码判断成败，支持重试；`keepalive: true` 保证页面关闭后请求继续
- **sendBeacon**：浏览器专为分析上报设计，页面卸载时最可靠，无返回值但保证"尽力发送"
- 两者的使用场景不同：正常运行时用 fetch（可重试），卸载时用 sendBeacon（最可靠）

### 7.3 为什么 IndexedDB 存批量而非逐条？

- 每次 `store.add()` 触发一次事务写入，批量存减少事务数
- 队列本身就以 batch 为单位 flush，一个 flush 批次 = 一条 DB 记录，自然对齐

### 7.4 为什么 Promise resolve 在 `tx.oncomplete` 而非 `add.onsuccess`？

- `add.onsuccess` 只表示操作被事务接受，事务可能尚未提交
- `tx.oncomplete` 表示事务真正提交，数据落盘

### 7.5 为什么 pagehide 不走异步 sender？

- 浏览器在 pagehide 后立即开始销毁进程
- `setTimeout` 回调被取消，后续 fetch 不会执行
- `saveToDB` 永远执行不到 → 数据静默丢失
- 解决：pagehide 专用快速通道 — `drainQueue()` → `saveToDB()` → `sendBeacon()`

### 7.6 为什么 `openDB()` 用单例缓存？

- `indexedDB.open()` 每次创建新连接，有开销
- 模块内多个函数（save/load/clear）共享同一连接
- `db.onclose` 事件中重置缓存，处理浏览器清除数据场景

### 7.7 为什么 `flush()` 先快照再清空？

- `flushCallback` 是异步的（sender.send 是 async 函数）
- 如果不清空，后续 `addToQueue` 追加的数据会与正在发送的批次混淆
- `[...queue]` 浅拷贝后 `queue.length = 0` 清空，保证数据隔离

---

## 8. 已知 Bug 与陷阱

### 8.1 TS 类型收窄陷阱（error.ts）

- **问题**：`window.addEventListener('error', (event) => {...})` 中 TS 推断 event 为 `ErrorEvent`
- **影响**：`if (event instanceof ErrorEvent) return` 后 TS 将 event 收窄为 `never`，无法访问 `event.target`
- **修复**：手动标注 `event: Event`

### 8.2 资源错误不冒泡

- **事实**：`<img>` / `<script>` / `<link>` 加载失败触发的 error 事件不冒泡
- **后果**：`window.addEventListener('error', handler)` 收不到（默认监听冒泡阶段）
- **修复**：必须 `window.addEventListener('error', handler, true)` 或 `{ capture: true }`

### 8.3 LCP `renderTime` vs `loadTime`

- **区别**：`loadTime` 是资源下载完成时刻，`renderTime` 是渲染到屏幕的时刻
- **策略**：优先取 `renderTime`，兜底取 `loadTime`（跨域图片且无 `Timing-Allow-Origin` 头时 `renderTime` 为 0）

### 8.4 file:// 协议下无法 fetch http://

- **原因**：浏览器的同源安全策略，`file://` 与 `http://` 是不同的安全源
- **修复**：使用 Express 同时托管测试页面和 API，全走 `http://localhost:3001`

---

## 9. 配置与构建

### 9.1 TypeScript 配置要点

| 选项 | 值 | 原因 |
|---|---|---|
| target | ES2017 | 原生 async/await，Chrome 58+ |
| module | ESNext | TS 不做模块转换，交给 Rollup |
| strict | true | 强制严格模式 |
| declaration | true | 生成 .d.ts |
| importHelpers | true | 从 tslib 引入 helper，减小体积 |
| moduleResolution | node10 | TS 6.x 兼容标记 |

### 9.2 Rollup 配置要点

| 选项 | 值 | 原因 |
|---|---|---|
| input | src/index.ts | 唯一入口 |
| output.format | iife | 浏览器 `<script>` 直接可用 |
| output.name | PerfMonitor | 全局变量名 |
| output.file | dist/perf-monitor.js | 产物路径 |
| sourcemap | true | 调试映射到 TS 源码 |
| plugins[0] | @rollup/plugin-typescript | 编译 TS |
| plugins[1] | @rollup/plugin-terser | 压缩（保留类名/函数名） |

### 9.3 npm 脚本

```json
{
    "build": "rollup -c rollup.config.js",
    "server": "node server/index.js"
}
```

---

## 10. 服务端 API

| 方法 | 路径 | Content-Type | 功能 |
|---|---|---|---|
| POST | `/report` | `application/json` | 接收 `TransportItem[]`，追加写入 `data.json` |
| GET | `/report/data` | `application/json` | 返回所有历史上报数据 |
| GET | `/` | `text/html` | 返回测试页面 |
| GET | `/dist/*` | — | 静态托管 SDK 产物 |

---

## 11. IndexedDB Schema

| 属性 | 值 |
|---|---|
| 数据库名 | `perf-monitor-db` |
| 版本号 | 1 |
| ObjectStore 名 | `pending-reports` |
| 主键 | `id`（自增） |
| 索引 | 无（当前版本不需要按条件查询） |
| 记录结构 | `{ id?: number, items: TransportItem[], timestamp: number }` |

---

## 12. 扩展点（已规划未实现）

| 扩展 | 文件 | 描述 |
|---|---|---|
| 长任务监控 | `metrics.ts` 新增 | `PerformanceObserver` type `longtask`，`entry.attribution[0]` 追溯脚本 |
| 数据采样 | `transport/sampler.ts` 新建 | 哈希采样：`hash(userId+url) % 100 < rate` |
| 可视化面板 | `test/dashboard.html` 新建 | Chart.js 折线图，实时展示 LCP/FID/CLS |
| 数据压缩 | `sender.ts` 修改 | `CompressionStream` gzip 压缩请求体 |
| 单元测试 | `test/*.test.ts` 新建 | Vitest 覆盖核心模块 |
