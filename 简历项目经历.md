# 简历项目经历 — perf-monitor

---

## 项目名称

**perf-monitor — 从零实现的前端性能监控 SDK**

---

## 项目概述

从零设计并实现了一个可直接用于生产环境的浏览器性能监控 SDK（TypeScript + Rollup，~500 行核心代码），覆盖 Core Web Vitals 三大指标采集、三类运行时错误监控，以及批量上报、弱网持久化、页面卸载兜底等完整上报链路，并配套 Node.js 接收服务与交互式测试页面。

---

## 技术栈

TypeScript · PerformanceObserver API · Fetch Keepalive · sendBeacon · IndexedDB · Page Visibility API · Rollup (IIFE) · Express

---

## 个人职责与成果

### 1. 架构设计

- 设计并实现 **四层架构**：采集层（metrics/error）→ 传输层（queue/sender）→ 持久层（IndexedDB storage）→ 兜底层（unload）
- 采用 **生产者-消费者模式**：采集模块通过 `addToQueue()` 解耦上报逻辑；队列通过 `onFlush(cb)` 回调注入发送器
- 定义 **判别联合类型体系**（`TransportItem = MetricData | ErrorData`），配合类型守卫函数实现编译期类型安全

### 2. 核心采集模块

- 使用 `PerformanceObserver` 实现 **LCP / FID / CLS** 三大指标的事件驱动采集，正确处理 `buffered`、`hadRecentInput` 等边界参数
- 实现 **三类错误监控**：JS 运行时错误（`window.onerror`）、资源加载错误（捕获阶段拦截，资源错误不冒泡）、Promise 未捕获异常（`unhandledrejection`），兼容跨浏览器降级

### 3. 上报策略与弱网兜底（技术难点）

- 实现 **双通道发送器**：`fetch + keepalive`（支持 HTTP 状态码判断与 3 次重试）+ `sendBeacon`（页面卸载时兜底）
- 设计 **错误去重策略**：基于 `Map<指纹, 时间戳>` 的滑动窗口去重，避免短时间内重复错误轰炸服务端
- 攻克 **页面卸载数据丢失**问题：发现 pagehide 中异步 sender 重试链（setTimeout + fetch）被浏览器取消导致数据静默丢失，设计双路径分流方案——visibilitychange 走异步重试路径，pagehide 走同步 drainQueue + IndexedDB + sendBeacon 快速通道
- 封装 **IndexedDB 离线队列**：Promise 化事件回调风格 API，单例缓存数据库连接，批量存储减少事务开销，init 时自动检测残留数据并补发

### 4. 工程化

- Rollup + TypeScript 打包为 IIFE 格式，`window.PerfMonitor.init()` 一行引入
- 编写 Node.js Express 接收服务（ES 模块），配套交互式测试页面覆盖正常采集、断网持久化、页面卸载等场景

---

## 项目亮点（面试可展开）

| 亮点 | 面试官可能的追问方向 |
|---|---|
| **双通道发送** | fetch keepalive 和 sendBeacon 的区别？为什么需要两个？keepalive 有什么限制？ |
| **页面卸载 Bug 修复** | 怎么发现这个问题的？浏览器页面生命周期？为什么 setTimeout 被取消？ |
| **IndexedDB 离线队列** | 为什么不用 localStorage？事务模型？为什么在 oncomplete 而不是 onsuccess 中 resolve？ |
| **PerformanceObserver** | buffered 参数的作用？LCP 为什么取 renderTime 而非 loadTime？CLS 的 hadRecentInput？ |
| **批量队列** | 为什么先快照再清空？生产者-消费者如何解耦？定时器和批量触发如何协调？ |

---

## 项目地址

GitHub: [github.com/sunyang498/perf-monitor](https://github.com/sunyang498/perf-monitor)

---

## 适用岗位

前端开发工程师（校招 / 实习）· 性能优化方向 · 监控/基础设施方向
